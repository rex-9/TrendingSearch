<%= turbo_frame_tag "trends" do %>
  <% trends.each do |trend| %>
    <%= link_to trend.keyword, trend_path(trend.id), class: "underline text-red-300" %>
    <%= trend.popularity %>
    <br>
  <% end %>
<% end %>
