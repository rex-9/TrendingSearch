<!DOCTYPE html>
<html>
  <head>
    <title>TrendingSearch</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <div class=" flex justify-center w-full bg-blue-200 px-5">
      <div>
        <% if notice %>
          <p class="bg-green-300 text-center py-2"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="bg-red-300 text-center py-2">alert<%= alert %></p>
        <% end %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
