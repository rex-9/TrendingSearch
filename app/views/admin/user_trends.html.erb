Trends of the
<%= @user.username %>
<% @trends.each do |trend| %>
  <div>
    <span class=""><%= trend.id %></span>
    <span class=""><%= trend.keyword %></span>
    <span class=""><%= trend.popularity %></span>
  </div>
<% end %>
